# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── 測試資料 ──
test_data/*.mp4
test_data/*.avi
test_data/*.mkv

# ── 模型檔案（太大不該進 git） ──
*.pt
*.onnx
*.engine
*.tflite
models/

# ── 日誌 ──
*.log
logs/

# ── 敏感資料 ──
.env
*.pem
*.key
credentials/

# ── 含 credential 的 config（TURN username/password、EC2 EIP、Tailscale hostname 等） ──
# test.yaml / test_h5.yaml 仍可進 repo（純 localhost，無敏感資訊）
config/staging.yaml
config/staging_video.yaml
config/prod.yaml
config/_runtime.yaml          # run_local_test.py 動態產生的暫存檔，不該進 repo

# ── OS ──
.DS_Store
Thumbs.db

# ── model ──

# ── Old Code ──
Code/

# ── Quick Test ──
quick_test/debug_frames/